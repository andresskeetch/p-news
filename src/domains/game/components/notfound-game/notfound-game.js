import React from "react";
import { Button, Title } from "common/components";

const NotFoundGame = () => {
  return (
    <div>
      <Title variant="h3">Story not found!!</Title>
      <Button label="Go Dashboard" />
    </div>
  );
};

export default NotFoundGame;
